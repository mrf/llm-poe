# Database
*.db
*.db-journal
*.db-shm
*.db-wal

# Lock files
*.lock

# Temporary
last-touched
*.tmp

# Local history backups
.br_history/

# DB recovery artifacts
.br_recovery/

# Sync state (local-only, per-machine)
.sync.lock
sync_base.jsonl

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# Runtime files
daemon.lock
daemon.log
daemon.pid
sync-state.json

# Worktree redirect file
redirect

# Backup data (legacy bd exports)
backup/

# Interactions log (runtime)
interactions.jsonl

# bv (beads viewer) lock file
.bv.lock
